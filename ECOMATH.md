# EcoMath — оқушы мен мұғалімге арналған экология + математика платформасы

Бұл құжат жобаның жаңартылған архитектурасын, Firestore құрылымын және іске қосу тәртібін сипаттайды.

## 1. Іске қосу

```bash
npm install          # немесе pnpm install
npm run dev          # http://localhost:5173
```

### `.env.local`

```env
NEXT_PUBLIC_FIREBASE_API_KEY=...
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=...
NEXT_PUBLIC_FIREBASE_PROJECT_ID=...
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=...
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=...
NEXT_PUBLIC_FIREBASE_APP_ID=...

# Мұғалім рөлін алуға қажет құпия код
NEXT_PUBLIC_TEACHER_INVITE_CODE=ECOMATH-TEACHER-2026
```

> ⚠️ Кодты өзгерткеннен кейін dev-серверді қайта іске қосыңыз (Vite env-ті старт кезінде оқиды).

### Firestore ережелерін жариялау

```bash
firebase deploy --only firestore:rules
```

`firestore.rules` жаңартылды — жаңа `assignments` коллекциясы мен мұғалімнің оқушы деректерін оқу
құқығы қосылды. Ережелерді жарияламасаңыз, мұғалім панелінде «Оқушылар тізімін жүктеу мүмкін болмады»
деген қате шығады.

### Қажетті Firestore индексі

Кері байланыс мұғалімге `orderBy("updatedAt")` арқылы жүктеледі — Firebase Console бірінші сұраныста
индекс сілтемесін консольге жазады, сол сілтемені басып индексті құрыңыз.

## 2. Рөлдер жүйесі (қауіпсіздік)

| Бұрын | Қазір |
|---|---|
| Кез келген адам тіркелгенде «Мұғалім» деп таңдай алатын | Мұғалім рөлі тек дұрыс **шақыру коды** енгізілгенде беріледі |
| Рөл localStorage арқылы ғана шешілетін | Код `ensureUserProfile` ішінде тексеріледі, `roleLocked: true` қойылады |
| `/teacher-dashboard` оқушыны үнсіз қайта бағыттайтын | Түсінікті «рұқсат жоқ» экраны көрсетіледі |

Firestore ережелері рөлдің кейін өзгеруіне тыйым салады (`request.resource.data.role == resource.data.role`).

## 3. Firestore құрылымы

```
users/{uid}
  displayName, email, photoURL, role, roleLocked,
  className, school, bio,
  createdAt, updatedAt, lastActiveAt,
  stats: {
    quizCount, bestPercent, avgPercent, lastPercent,
    totalCorrect, totalQuestions, xp, streak, lastQuizAt,
    assignmentsSubmitted, assignmentsGraded, gradeSum
  }
  └── quizAttempts/{id}
        score, total, percent, topicId, topicTitle, durationSec, createdAt

assignments/{id}
  teacherId, teacherName, title, description, topic,
  targetClass, maxPoints, dueAt, submissionCount, gradedCount, createdAt
  └── submissions/{studentId}
        studentId, studentName, className, text, link,
        status ("submitted" | "graded"), grade, maxPoints,
        feedback, submittedAt, gradedAt, gradedBy

feedbackThreads/{id}
  studentId, studentName, studentClass, subject, category,
  status ("open" | "answered" | "closed"),
  lastMessage, lastSenderRole, messageCount, createdAt, updatedAt
  └── messages/{id}
        senderId, senderName, senderRole, text, createdAt
```

`stats` өрісі профиль құжатында жиынтық түрде сақталады — сондықтан мұғалім панелі әр оқушының
ішкі коллекциясын оқымай-ақ, бір сұраныспен бүкіл сынып статистикасын көрсете алады.

## 4. Беттер

| Маршрут | Кім көреді | Не бар |
|---|---|---|
| `/` | барлығы | Хиро, оқу жолы, жүйе қалай жұмыс істейді |
| `/methods` | барлығы | 4 математикалық әдіс |
| `/lab` | барлығы | **4 калькулятор**: пластик, су, энергия, қағаз |
| `/project` | барлығы | Ғылыми жоба жоспары |
| `/quiz` | кірген қолданушы | **4 тақырып + аралас режим**, таймер, XP, серия |
| `/assignments` | оқушы | Тапсырмалар, жұмыс жіберу, баға мен мұғалім пікірі |
| `/feedback` | кірген қолданушы | Чат, іздеу, статус сүзгісі |
| `/profile` | **оқушы** | Деңгей, XP, төсбелгі, тақырып талдауы, бағалар, профильді өңдеу |
| `/profile` | **мұғалім** | Бөлек дизайн: оқушы саны, сыныптар, соңғы оқиғалар, қолдау қажет оқушылар, бағалау сапасы |
| `/teacher-dashboard` | **тек мұғалім** | 5 таб: Шолу, Оқушылар, Тапсырмалар, Тексеру, Кері байланыс |

## 5. Мұғалім панелі (толық қайта жазылған)

Бұрын мұғалім панелінде оқушының жеке статистикасы (өз викторина нәтижелері) көрсетілетін —
бұл қате болатын. Қазір ол **оқушылар туралы** деректерді көрсетеді:

- **Шолу** — барлық оқушы саны, 7 күндегі белсенділер, сынып ортасы, тексеруді күтетін жұмыстар,
  үздік оқушылар рейтингі, сынып бойынша үлгерім диаграммасы, «60%-дан төмен оқушылар» ескертуі.
- **Оқушылар** — іздеу + сынып сүзгісі бар кесте: аты, сынып, тест саны, орташа, үздік, XP, соңғы белсенділік.
- **Тапсырмалар** — жаңа тапсырма құру (тақырып, сынып, макс. балл, мерзім), жіберілім/тексерілім санағышы.
- **Тексеру** — оқушылар жіберген жұмыстар, баға қою + мәтіндік пікір жазу.
- **Кері байланыс** — оқушылардың сұрақтарына жауап беру чаты.

## 6. Оқушы тарапындағы кері байланыс циклі

1. Мұғалім `/teacher-dashboard → Тапсырмалар` бетінде тапсырма жариялайды.
2. Оқушы `/assignments` бетінде оны көреді (сынып сүзгісі ескеріледі), жауабын және сілтемесін жібереді.
3. Мұғалім `Тексеру` табында бағалайды әрі пікір жазады.
4. Оқушы бағаны `/assignments` пен `/profile` бетінде көреді, XP алады (баға × 10).
5. Түсінбеген жері болса — `/feedback` арқылы тікелей сұрақ қояды.

## 7. Геймификация

- **XP**: дұрыс жауап × 20, 100% үшін +60, 80%+ үшін +30, баға × 10.
- **Деңгей**: әр 250 XP сайын. Атаулары: Бастаушы → Бақылаушы → Тәжірибеші → Зерттеуші → Аналитик → Эко-сарапшы → Эко-шебер.
- **Серия (streak)**: күн сайын тест тапсырса өседі.
- **8 төсбелгі**: `lib/achievements.ts` ішінде.

## 8. Жөнделген кемшіліктер

1. Мұғалім dashboard-ында оқушының жеке деректері көрсетілетін → енді оқушылар жайлы дерек.
2. Кез келген адам мұғалім бола алатын → шақыру коды енгізілді.
3. Тіркелуде аты-жөні, сынып, мектеп сұралмайтын → қосылды.
4. Құпиясөзді растау жоқ, «ұмыттым» функциясы жоқ → қосылды.
5. Құпиясөз күшінің индикаторы жоқ → қосылды.
6. Қате хабарламалар аз (`invalid-email`, `network-request-failed`, `too-many-requests`) → толықтырылды.
7. `FeedbackCenter` профильді өз бетінше қайта жүктейтін (артық сұраныс) → `useAuth()` профилін пайдаланады.
8. Профиль статистикасы тек `quizAttempts`-тен есептелетін → `users/{uid}.stats` жиынтығы қосылды.
9. Профильді өңдеу мүмкіндігі жоқ → аты, сынып, мектеп, био өңделеді.
10. Викторина 5 қатаң сұрақтан тұратын → 4 тақырып, 24+ сұрақ, араластыру, таймер.
11. Зертханада бір ғана калькулятор бар → 4 калькулятор.
12. Мұғалімге `/teacher-dashboard` қолжетімсіз болса ақ экран көрсетілетін → түсінікті хабарлама.
13. Мобильді мәзір маршрут ауысқанда жабылмайтын → жабылады.
14. Firestore ережелерінде `list` рұқсаты болмағандықтан мұғалім сұранысы қате беретін → қосылды.
15. Хедердегі қолданушы картасы мұғалімді панельге апаратын → енді әрқашан `/profile` бетіне апарады.
16. Мұғалімнің профилі оқушынікімен бірдей болатын → **бөлек мұғалім профилі** жасалды (`components/teacher-profile.tsx`).
17. Мұғалімнің оқытатын пәні сақталмайтын → `subject` өрісі қосылды (тіркелу формасында да, профильді өңдеуде де).

## 9. Екі бөлек профиль беті

`/profile` маршруты рөлге қарай екі түрлі компонент көрсетеді:

- **`components/student-profile.tsx`** — оқушыға: XP/деңгей, төсбелгілер, викторина тарихы,
  тақырып бойынша күшті/әлсіз жақтар, мұғалім қойған бағалар, «Келесі мақсат» сақинасы.
- **`components/teacher-profile.tsx`** — мұғалімге мүлде басқа мазмұн мен дизайн:
  - Қою жасыл градиентті хиро: пән, мектеп, жетекші сынып белгілері
  - 4 метрика: оқушылар саны (белсенділермен), тапсырмалар, тексерілген жұмыстар, сынып ортасы
  - «Менің сыныптарым» — сынып бойынша қамту диаграммасы
  - «Соңғы оқиғалар» — оқушылардың жіберілімдері мен сұрақтарының таспасы
  - «Қолдау қажет оқушылар» — орташасы 60%-дан төмен оқушылар тізімі
  - Кері байланыс статистикасы (жауап беру деңгейі) және бағалау сапасы сақинасы
  - Әдістемелік кеңес картасы

Мұғалім профилінде **XP, деңгей, төсбелгі, викторина тарихы жоқ** — олар тек оқушыға арналған.
